[project]
name = "badapples"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "mlx-lm>=0.31.3",
    "openai>=3.19.2",
    "pyarrow>=25.0.1",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
